apiVersion: v1
kind: ServiceAccount
metadata:
  name: care-plan-validator
  namespace: prism
  annotations:
    iam.gke.io/gcp-service-account: prism-ml-workload@athena-prod-484720.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: care-plan-validator
  namespace: prism
  labels:
    app: care-plan-validator
    tier: ml
spec:
  replicas: 2
  selector:
    matchLabels:
      app: care-plan-validator
  template:
    metadata:
      labels:
        app: care-plan-validator
        tier: ml
    spec:
      serviceAccountName: care-plan-validator
      containers:
        - name: care-plan-validator
          image: care-plan-validator
          ports:
            - containerPort: 8102
          env:
            - name: PORT
              value: "8102"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: DATABASE_URL
            - name: LOG_LEVEL
              value: "INFO"
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8102
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8102
            initialDelaySeconds: 30
            periodSeconds: 10
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.8.0
          args:
            - "--structured-logs"
            - "--auto-iam-authn"
            - "athena-prod-484720:us-central1:prism-db"
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: care-plan-validator
  namespace: prism
spec:
  selector:
    app: care-plan-validator
  ports:
    - port: 8102
      targetPort: 8102
  type: ClusterIP
