apiVersion: v1
kind: ServiceAccount
metadata:
  name: organization-service
  namespace: prism
  annotations:
    iam.gke.io/gcp-service-account: prism-workload@athena-prod-484720.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: organization-service
  namespace: prism
  labels:
    app: organization-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: organization-service
  template:
    metadata:
      labels:
        app: organization-service
        tier: backend
    spec:
      serviceAccountName: organization-service
      containers:
        - name: organization-service
          image: organization-service
          ports:
            - containerPort: 4002
          envFrom:
            - configMapRef:
                name: prism-config
          env:
            - name: PORT
              value: "4002"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: JWT_SECRET
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 4002
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 5
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.8.0
          args:
            - "--structured-logs"
            - "--auto-iam-authn"
            - "athena-prod-484720:us-central1:prism-db"
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: organization-service
  namespace: prism
spec:
  selector:
    app: organization-service
  ports:
    - port: 4002
      targetPort: 4002
  type: ClusterIP
